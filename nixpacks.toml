[variables]
NPM_CONFIG_PRODUCTION = "false"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs"]
cmds = ["npm install", "npm --prefix backend install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node backend/server.js"
